server {
    listen 8080;
    server_name localhost;
    root ./www;
    location / { methods GET; }
}

server {
    listen 9090;
    server_name admin;
    root ./www;
    location / { methods GET; }
}